<h1>Products (<%= @products.length %>)</h1>
<%= link_to "New Product", "/products/new" %>

<% @products.each do |product| %>
<p><%= product[:name] %>, <%= product[:description] %>, <%= product[:price_in_cents] %></p>
<%= link_to image_tag("icon"), "/products/#{product.id}"  %>
<% end %>